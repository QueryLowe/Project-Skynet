<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical">

    <TextView
        android:id="@+id/textView"
        android:layout_width="match_parent"
        android:layout_height="51dp"
        android:text="Explore more workouts:"
        android:textAlignment="center"
        android:textAppearance="@style/TextAppearance.AppCompat.Large"
        android:textColor="@color/colorPrimary"
        android:textStyle="bold" />

    <ListView
        android:id="@+id/explore_list_view"
        android:layout_width="match_parent"
        android:layout_height="563dp"></ListView>

    <Button
        android:id="@+id/explore_start"
        android:layout_width="180dp"
        android:layout_height="wrap_content"
        android:layout_gravity="center_horizontal"
        android:backgroundTint="#0AEE11"
        android:text="Start"
        android:textColor="#FFFFFF" />

    <Button
        android:id="@+id/explore_cancel"
        android:layout_width="180dp"
        android:layout_height="wrap_content"
        android:layout_gravity="center_horizontal"
        android:backgroundTint="#FF1200"
        android:text="Cancel"
        android:textColor="#FFFFFF" />

</LinearLayout>